---
layout: null
permalink: /feed/
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" {% if site.lang %}xml:lang="{{ site.lang }}"{% endif %}>
    <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
    <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
    <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" {% if site.lang %}hreflang="{{ site.lang }}" {% endif %}/>
    <updated>{{ site.time | date_to_xmlschema }}</updated>
    <id>{{ '/' | absolute_url | xml_escape }}</id>
    <title type="html">{{ site.title | smartify | xml_escape }}</title>
    <subtitle>{{ site.description | xml_escape }}</subtitle>

    <author>
      <name>{{ site.author.name | xml_escape }}</name>
      <email>{{ site.author.email | xml_escape }}</email>
    </author>

    {% assign allPosts = site.posts | concat: site.medium_feed | sort: 'date' | reverse %}
    {% for post in allPosts limit:10 %}
      <entry>
        <title type="html">{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
        <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
        <published>{{ post.date | date_to_xmlschema }}</published>
        <updated>{{ post.updated | default: post.date | date_to_xmlschema }}</updated>
        <id>{{ post.url | absolute_url | xml_escape }}</id>
        <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}">{{ post.feed_content | default: post.content | strip | xml_escape }}</content>

        <author>
          <name>{{ post.author | default: site.author.name | xml_escape }}</name>
          <email>{{ post.email | default: site.author.email | xml_escape }}</email>
        </author>

        {% for cat in post.categories %}
          <category term="{{ cat | xml_escape }}" />
        {% endfor %}

        {% if post.image %}
          {% unless post.image contains "://" %}
            {% assign post.image = post.image | absolute_url %}
          {% endunless %}
          <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ post.image | xml_escape }}" />
        {% endif %}
      </entry>
    {% endfor %}
</feed>
